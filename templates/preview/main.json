{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "metadata": {
    "_generator": {
      "name": "bicep",
      "version": "0.40.2.10011",
      "templateHash": "16583130045051148670"
    }
  },
  "parameters": {
    "networkingMode": {
      "type": "string",
      "defaultValue": "public",
      "allowedValues": [
        "public",
        "internal"
      ],
      "metadata": {
        "description": "Public for default Azure subscriptions. Internal for MSFT-internal subs that require peered VNet + apiserver-ILB and have NRMS NSG policy applied."
      }
    },
    "ciVersion": {
      "type": "string",
      "defaultValue": "v1.37.0-alpha.0",
      "metadata": {
        "description": "Kubernetes alpha CI version (e.g. v1.37.0-alpha.0). Pick from https://github.com/kubernetes/kubernetes/releases (the alpha stream lives on the next minor; 1.36.x is in RC, so alpha features land in 1.37+)."
      }
    },
    "featureGatesOverride": {
      "type": "string",
      "defaultValue": "",
      "metadata": {
        "description": "Override the default AllAlpha/AllBeta feature-gate string. Leave blank to use the defaults."
      }
    },
    "extraFeatureRecipes": {
      "type": "string",
      "defaultValue": "",
      "metadata": {
        "description": "Space-separated list of opt-in feature recipes to apply (filenames in templates/preview/features without the .yaml). Currently supports: dra."
      }
    },
    "controlPlaneMachineCount": {
      "type": "int",
      "defaultValue": 1,
      "allowedValues": [
        1,
        3,
        5
      ],
      "metadata": {
        "description": "Replica count for the workload cluster control plane. Internal mode is auto-bumped to 3 at deploy time for apiserver-ILB hairpin routing if you leave this at 1."
      }
    },
    "workerMachineCount": {
      "type": "int",
      "defaultValue": 2,
      "minValue": 0,
      "maxValue": 10,
      "metadata": {
        "description": "Replica count for the workload cluster worker MachineDeployment."
      }
    },
    "aksKubernetesVersion": {
      "type": "string",
      "defaultValue": "1.33.2",
      "metadata": {
        "description": "AKS Kubernetes version for the management cluster (NOT the workload cluster)."
      }
    },
    "aksNodeVmSize": {
      "type": "string",
      "defaultValue": "Standard_D2s_v3",
      "metadata": {
        "description": "AKS node VM size. Defaults to Standard_D2s_v3 because Standard_B2s and other burstable SKUs are policy-restricted in some subscriptions/regions and fail AKS preflight validation with \"VM size … is not allowed in your subscription in location …\". Switch to a burstable SKU (e.g. Standard_B2s) on subs that allow it to save cost."
      }
    },
    "aksNodeCount": {
      "type": "int",
      "defaultValue": 2,
      "minValue": 1,
      "maxValue": 5,
      "metadata": {
        "description": "AKS node count."
      }
    },
    "workloadControlPlaneVmSize": {
      "type": "string",
      "defaultValue": "Standard_D2s_v3",
      "metadata": {
        "description": "Control plane VM size for the workload cluster."
      }
    },
    "workloadNodeVmSize": {
      "type": "string",
      "defaultValue": "Standard_D2s_v3",
      "metadata": {
        "description": "Worker VM size for the workload cluster."
      }
    },
    "mgmtVnetCidr": {
      "type": "string",
      "defaultValue": "10.100.0.0/16",
      "metadata": {
        "description": "CIDR for the AKS management VNet. Only used in internal mode."
      }
    },
    "aksSubnetCidr": {
      "type": "string",
      "defaultValue": "10.100.0.0/24",
      "metadata": {
        "description": "CIDR for the AKS subnet. Only used in internal mode."
      }
    },
    "workloadVnetCidr": {
      "type": "string",
      "defaultValue": "10.200.0.0/16",
      "metadata": {
        "description": "CIDR for the workload cluster VNet. Only used in internal mode."
      }
    },
    "workloadCpSubnetCidr": {
      "type": "string",
      "defaultValue": "10.200.0.0/24",
      "metadata": {
        "description": "CIDR for the workload cluster control plane subnet. Only used in internal mode."
      }
    },
    "workloadNodeSubnetCidr": {
      "type": "string",
      "defaultValue": "10.200.1.0/24",
      "metadata": {
        "description": "CIDR for the workload cluster node subnet. Only used in internal mode."
      }
    },
    "workloadApiserverIlbIp": {
      "type": "string",
      "defaultValue": "10.200.0.100",
      "metadata": {
        "description": "Private IP for the workload cluster apiserver ILB frontend. Must be within workloadCpSubnetCidr and outside its first 4 addresses. Only used in internal mode."
      }
    },
    "previewRepoUrl": {
      "type": "string",
      "defaultValue": "https://github.com/willie-yao/cluster-api-provider-azure.git",
      "metadata": {
        "description": "Repository hosting the templates/preview tree (kustomize + features). The deploymentScript clones this at the ref below. Defaults point at the preview fork until this lands upstream."
      }
    },
    "previewRepoRef": {
      "type": "string",
      "defaultValue": "alpha-features",
      "metadata": {
        "description": "Git ref (branch or tag) to clone from previewRepoUrl. Pin to a release tag for reproducible deploys."
      }
    },
    "workloadClusterName": {
      "type": "string",
      "defaultValue": "capz-preview",
      "metadata": {
        "description": "Workload cluster name (also used as the Cluster CR name)."
      }
    }
  },
  "variables": {
    "$fxv#0": "#!/usr/bin/env bash\n# Bootstrap script run inside the Bicep deploymentScripts ACI container.\n# Connects to the AKS mgmt cluster, installs CAPI Operator (with CAPZ +\n# Helm addon), applies the alpha-preview workload cluster, and (in\n# internal mode) patches NRMS NSG rules to allow CAPI bring-up.\n\nset -o errexit\nset -o nounset\nset -o pipefail\n\n# Inputs (passed in via Bicep environmentVariables):\n#   AKS_NAME, AKS_RG, AKS_NODE_RG\n#   UAMI_CLIENT_ID\n#   AZURE_LOCATION, AZURE_SUBSCRIPTION_ID, AZURE_TENANT_ID\n#   CLUSTER_NAME, CLUSTER_NAMESPACE, CI_VERSION\n#   K8S_FEATURE_GATES (optional, defaults to AllAlpha=true,AllBeta=true)\n#   K8S_RUNTIME_CONFIG (optional, defaults to api/all=true)\n#   CONTROL_PLANE_MACHINE_COUNT, WORKER_MACHINE_COUNT\n#   NETWORKING_MODE        (public | internal)\n#   AZURE_VNET_CIDR, AZURE_CP_SUBNET_CIDR, AZURE_NODE_SUBNET_CIDR  (internal only)\n#   AZURE_INTERNAL_LB_PRIVATE_IP                                   (internal only)\n#   APISERVER_LB_DNS_SUFFIX                                        (internal only)\n#   EXTRA_FEATURE_RECIPES  (space-separated list of files in features/)\n\n# Pinned component versions. Bump together in CAPZ release PRs.\n# Phase 0 verification matrix (validated 2026-05-21):\n#   - operator v0.27.0 is the first line that accepts v1beta2 provider release\n#     contracts (v0.21 hard-rejects). See operator phases.go::validateRepoCAPIVersion.\n#   - CAPI v1.13.2 ships v1beta2 metadata; serves v1beta1 via conversion webhook.\n#   - CAPZ v1.24.1 is the matching CAPZ release for CAPI v1.13.x and includes\n#     the APIServerILB feature gate (added in v1.18.0).\n#   - helm-addon v0.6.4 matches the modern CAPI line.\nCAPI_VERSION=\"${CAPI_VERSION:-v1.13.2}\"\nCAPZ_VERSION=\"${CAPZ_VERSION:-v1.24.1}\"\nCAPI_OPERATOR_VERSION=\"${CAPI_OPERATOR_VERSION:-v0.27.0}\"\nHELM_ADDON_VERSION=\"${HELM_ADDON_VERSION:-v0.6.4}\"\nCERT_MANAGER_VERSION=\"${CERT_MANAGER_VERSION:-v1.15.3}\"\n\n# az_retry and wait_and_fix_nsg_rules are sourced from scripts/peer-vnets.sh\n# inside fetch_preview_tree (after the repo is cloned). That script has a\n# bottom-of-file sourcing guard so its main() does not run on source.\n\n# Wait for a Kubernetes resource to exist before kubectl waits on its status.\n# kubectl wait fails immediately if the object isn't there yet, which is\n# common during CAPI Operator install when CRDs are still being applied.\nwait_for_object() {\n    local kind=\"$1\" name=\"$2\" ns=\"${3:-default}\" timeout=\"${4:-300}\"\n    local deadline=$(( $(date +%s) + timeout ))\n    while ! kubectl get \"${kind}\" \"${name}\" -n \"${ns}\" >/dev/null 2>&1; do\n        if (( $(date +%s) > deadline )); then\n            echo \"timed out waiting for ${kind}/${name} in ns ${ns}\" >&2\n            return 1\n        fi\n        sleep 5\n    done\n}\n\n# Install a missing CLI tool from the Alpine package repo (the\n# mcr.microsoft.com/azure-cli image is Alpine-based). Used to add tools the\n# image doesn't bundle (e.g. git, helm) without rebuilding a custom image.\n# Progress goes to stderr so callers that capture stdout (e.g.\n# ensure_ssh_keypair) don't pull \"installing X via apk\" into return values.\nensure_tool() {\n    local tool=\"$1\"\n    if command -v \"${tool}\" >/dev/null 2>&1; then\n        return 0\n    fi\n    echo \"  installing ${tool} via apk\" >&2\n    apk add --no-cache --quiet \"${tool}\" >&2\n}\n\n# Generate or fetch a stable SSH keypair for the workload cluster nodes and\n# emit the base64-encoded public key on stdout. Persisting as a Kubernetes\n# Secret in the mgmt cluster keeps the value stable across re-runs of the\n# bootstrap script so AzureMachineTemplate's immutable\n# spec.template.spec.sshPublicKey doesn't change between deploys.\n# The private key is stored alongside so an operator can SSH into a node\n# for debugging via:\n#   kubectl get secret -n default ${CLUSTER_NAME}-ssh-cache \\\n#     -o jsonpath='{.data.private-key}' | base64 -d > ~/.ssh/capz-preview-key\nensure_ssh_keypair() {\n    ensure_tool openssh-keygen\n    local secret_name=\"${CLUSTER_NAME}-ssh-cache\"\n    local cached\n    cached=$(kubectl -n \"${CLUSTER_NAMESPACE:-default}\" get secret \"${secret_name}\" \\\n        -o jsonpath='{.data.public-key-b64}' 2>/dev/null | base64 -d || true)\n    if [[ -n \"${cached}\" ]]; then\n        echo \"${cached}\"\n        return 0\n    fi\n    local tmpdir; tmpdir=$(mktemp -d)\n    ssh-keygen -t rsa -b 2048 -N \"\" -f \"${tmpdir}/id_rsa\" -C \"capz-preview\" >/dev/null\n    local b64\n    b64=$(base64 -w 0 < \"${tmpdir}/id_rsa.pub\")\n    kubectl -n \"${CLUSTER_NAMESPACE:-default}\" create secret generic \"${secret_name}\" \\\n        --from-literal=public-key-b64=\"${b64}\" \\\n        --from-file=private-key=\"${tmpdir}/id_rsa\" \\\n        --dry-run=client -o yaml | kubectl apply -f - >&2\n    rm -rf \"${tmpdir}\"\n    echo \"${b64}\"\n}\n\nmain() {\n    echo \"================================================================\"\n    echo \"CAPZ alpha-k8s preview bootstrap\"\n    echo \"  cluster:      ${CLUSTER_NAME}\"\n    echo \"  ci version:   ${CI_VERSION}\"\n    echo \"  mode:         ${NETWORKING_MODE}\"\n    echo \"  location:     ${AZURE_LOCATION}\"\n    echo \"================================================================\"\n\n    fetch_preview_tree\n    connect_to_aks\n    install_cert_manager\n    install_capi_operator\n    annotate_capz_workload_identity\n    apply_workload_cluster\n    apply_feature_recipes\n    if [[ \"${NETWORKING_MODE}\" == \"internal\" ]]; then\n        fix_nrms_nsg_rules\n    fi\n    wait_for_workload_cluster_ready\n\n    echo \"================================================================\"\n    echo \"bootstrap complete. retrieve the workload kubeconfig with:\"\n    echo \"  az aks get-credentials -g ${AKS_RG} -n ${AKS_NAME} --admin --overwrite-existing\"\n    echo \"  kubectl get secret -n ${CLUSTER_NAMESPACE:-default} ${CLUSTER_NAME}-kubeconfig \\\\\"\n    echo \"    -o jsonpath='{.data.value}' | base64 -d > workload.kubeconfig\"\n    echo \"  kubectl --kubeconfig workload.kubeconfig get nodes\"\n    echo \"================================================================\"\n}\n\n# Bicep inlines this script via loadTextContent(), but the kustomize tree\n# and feature recipes are not inlined. Clone the CAPZ repo at the pinned\n# ref so paths like overlays/<mode> and features/<recipe>.yaml resolve.\nfetch_preview_tree() {\n    local repo_url=\"${PREVIEW_REPO_URL:-https://github.com/kubernetes-sigs/cluster-api-provider-azure.git}\"\n    local repo_ref=\"${PREVIEW_REPO_REF:-main}\"\n    echo \"--- fetching preview tree from ${repo_url}@${repo_ref} ---\"\n\n    # The Azure deploymentScript azureCLI image at 2.61.0 ships az + a stripped\n    # Alpine base. It does NOT bundle git, helm, curl, or kubectl. Install the\n    # full toolchain up-front from Alpine's package repo so subsequent steps\n    # don't need to retry one tool at a time.\n    ensure_tool git\n    ensure_tool helm\n    ensure_tool curl\n    ensure_tool kubectl\n    rm -rf /tmp/capz\n    git clone --depth=1 --branch \"${repo_ref}\" \"${repo_url}\" /tmp/capz\n    cd /tmp/capz/templates/preview\n\n    # Reuse az_retry, wait_and_fix_nsg_rules, peer_vnets, etc. from the\n    # canonical peer-vnets.sh. The bottom-of-file `BASH_SOURCE == $0`\n    # guard prevents its main() from running when sourced.\n    # shellcheck disable=SC1091\n    source /tmp/capz/scripts/peer-vnets.sh\n\n    install_kustomize\n    install_clusterctl\n}\n\ninstall_kustomize() {\n    if command -v kustomize >/dev/null 2>&1; then\n        return 0\n    fi\n    echo \"  installing kustomize\"\n    curl -sSL -o /tmp/install_kustomize.sh \\\n        \"https://raw.githubusercontent.com/kubernetes-sigs/kustomize/master/hack/install_kustomize.sh\"\n    bash /tmp/install_kustomize.sh 5.4.3 /usr/local/bin\n}\n\n# clusterctl is used for variable substitution into the rendered manifests.\n# envsubst is the wrong tool: it does not understand the `${VAR:=default}`\n# syntax (leaves it as a literal) and worse, it mangles `$${VAR}` (which\n# our embedded cloud-init scripts use to defer expansion until node boot).\ninstall_clusterctl() {\n    if command -v clusterctl >/dev/null 2>&1; then\n        return 0\n    fi\n    echo \"  installing clusterctl ${CAPI_VERSION}\"\n    curl -sSL -o /usr/local/bin/clusterctl \\\n        \"https://github.com/kubernetes-sigs/cluster-api/releases/download/${CAPI_VERSION}/clusterctl-linux-amd64\"\n    chmod +x /usr/local/bin/clusterctl\n}\n\nconnect_to_aks() {\n    echo \"--- fetching AKS credentials ---\"\n    az_retry 5 az aks get-credentials \\\n        --resource-group \"${AKS_RG}\" \\\n        --name \"${AKS_NAME}\" \\\n        --overwrite-existing --only-show-errors\n}\n\ninstall_cert_manager() {\n    echo \"--- installing cert-manager ${CERT_MANAGER_VERSION} ---\"\n    helm repo add jetstack https://charts.jetstack.io --force-update\n    helm repo update\n    helm upgrade --install cert-manager jetstack/cert-manager \\\n        --namespace cert-manager --create-namespace \\\n        --version \"${CERT_MANAGER_VERSION}\" \\\n        --set crds.enabled=true \\\n        --wait --timeout 5m\n}\n\ninstall_capi_operator() {\n    echo \"--- installing CAPI Operator ${CAPI_OPERATOR_VERSION} (CAPZ=${CAPZ_VERSION}) ---\"\n    helm repo add capi-operator https://kubernetes-sigs.github.io/cluster-api-operator\n    helm repo update\n\n    # cluster-api-operator v0.21+ requires provider name as the YAML key with\n    # an optional `version` field. The pre-v0.20 string shorthand\n    # (\"cluster-api:vX.Y.Z\") is rejected by the chart's JSON schema.\n    # bootstrap+controlPlane (kubeadm) are required because the workload\n    # cluster manifests reference KubeadmControlPlane and KubeadmConfigTemplate.\n    cat > /tmp/capi-operator-values.yaml <<EOF\ncore:\n  cluster-api:\n    version: ${CAPI_VERSION}\n    manager:\n      featureGates:\n        ClusterTopology: true\n        MachinePool: true\nbootstrap:\n  kubeadm:\n    version: ${CAPI_VERSION}\ncontrolPlane:\n  kubeadm:\n    version: ${CAPI_VERSION}\ninfrastructure:\n  azure:\n    namespace: capz-system\n    version: ${CAPZ_VERSION}\n    configSecret:\n      name: azure-variables\n      namespace: capz-system\n    manager:\n      featureGates:\n        # APIServerILB is alpha in CAPZ and required for the internal\n        # overlay's apiServerLB.frontendIPs (public + private) to work.\n        # Added in CAPZ v1.18.0; CAPZ_VERSION must be >= v1.18.0.\n        APIServerILB: true\n        MachinePool: true\naddon:\n  helm:\n    version: ${HELM_ADDON_VERSION}\nEOF\n\n    # Pre-create the configSecret namespace + an empty secret that CAPZ\n    # reads identity material from. We use Workload Identity so the secret\n    # itself is empty; the UAMI client ID is wired in via SA annotation.\n    # The CAPI Operator chart manages the capz-system namespace + the\n    # InfrastructureProvider CR. We create the configSecret AFTER the helm\n    # install so that:\n    #  - helm doesn't try to adopt/overwrite the namespace mid-flight, and\n    #  - the InfraProvider's secret lookup happens against a stable cluster.\n    # The operator will retry the InfraProvider reconcile until the secret\n    # appears, so create-after-helm is safe.\n    helm upgrade --install capi-operator capi-operator/cluster-api-operator \\\n        --namespace capi-operator-system --create-namespace \\\n        --version \"${CAPI_OPERATOR_VERSION}\" \\\n        -f /tmp/capi-operator-values.yaml \\\n        --wait --timeout 15m\n\n    echo \"--- creating azure-variables configSecret in capz-system ---\"\n    # The helm chart already created the namespace (createNamespace defaults true).\n    # Just create the secret CAPZ + ASO read identity config from. Workload\n    # Identity means there is no client secret — the UAMI client ID is wired\n    # in via SA annotation in a later step.\n    kubectl create namespace capz-system --dry-run=client -o yaml | kubectl apply -f -\n    kubectl create secret generic azure-variables -n capz-system \\\n        --from-literal=AZURE_CLIENT_ID=\"${UAMI_CLIENT_ID}\" \\\n        --from-literal=AZURE_TENANT_ID=\"${AZURE_TENANT_ID}\" \\\n        --from-literal=AZURE_SUBSCRIPTION_ID=\"${AZURE_SUBSCRIPTION_ID}\" \\\n        --dry-run=client -o yaml | kubectl apply -f -\n\n    echo \"--- waiting for CAPI + CAPZ controllers to become Available ---\"\n    wait_for_object deployment capi-controller-manager capi-system 600\n    kubectl -n capi-system wait --for=condition=Available \\\n        deployment/capi-controller-manager --timeout=10m\n    wait_for_object deployment capz-controller-manager capz-system 600\n    kubectl -n capz-system wait --for=condition=Available \\\n        deployment/capz-controller-manager --timeout=10m\n}\n\nannotate_capz_workload_identity() {\n    echo \"--- annotating capz-manager + ASO SAs for Workload Identity ---\"\n    kubectl -n capz-system annotate serviceaccount capz-manager \\\n        \"azure.workload.identity/client-id=${UAMI_CLIENT_ID}\" --overwrite\n\n    # ASO ships alongside CAPZ in modern versions; annotate it too so its\n    # reconciles use Workload Identity when the user picks an ASO-backed\n    # feature recipe. Safe to be a no-op if the SA doesn't exist.\n    if kubectl -n capz-system get serviceaccount azureserviceoperator-default >/dev/null 2>&1; then\n        kubectl -n capz-system annotate serviceaccount azureserviceoperator-default \\\n            \"azure.workload.identity/client-id=${UAMI_CLIENT_ID}\" --overwrite\n        kubectl -n capz-system label serviceaccount azureserviceoperator-default \\\n            \"azure.workload.identity/use=true\" --overwrite\n    fi\n\n    kubectl -n capz-system rollout restart deployment/capz-controller-manager\n    kubectl -n capz-system rollout status  deployment/capz-controller-manager --timeout=5m\n}\n\napply_workload_cluster() {\n    echo \"--- rendering and applying workload cluster (overlay=${NETWORKING_MODE}) ---\"\n\n    # The Bicep deploymentScript supportingScriptUris dumps the entire\n    # templates/preview tree into $PWD. Pick the overlay for this mode.\n    local overlay=\"overlays/${NETWORKING_MODE}\"\n    if [[ ! -d \"${overlay}\" ]]; then\n        echo \"overlay directory '${overlay}' not found\" >&2\n        exit 1\n    fi\n\n    export CLUSTER_NAME CLUSTER_IDENTITY_NAME=\"${CLUSTER_NAME}-identity\"\n    export AZURE_LOCATION AZURE_SUBSCRIPTION_ID AZURE_TENANT_ID\n    export AZURE_CLIENT_ID=\"${UAMI_CLIENT_ID}\"\n    export AZURE_RESOURCE_GROUP=\"${AKS_RG}\"\n    # KCP and MachineDeployment .spec.version (the field CAPI validates) must\n    # be a bare semver like \"v1.37.0-alpha.0\". CAPI v1.13's defaulter\n    # auto-prepends \"v\" to any version not starting with \"v\", which corrupts\n    # the historical \"ci/v1.X.Y-foo\" sentinel into \"vci/v1.X.Y-foo\" and the\n    # semver validator then rejects it. The \"ci/\" prefix still belongs in the\n    # kubeadm clusterConfiguration.kubernetesVersion (set by\n    # base/ci-version-patch.yaml) where kubeadm uses it to resolve images\n    # against dl.k8s.io/ci/<ver>.\n    export KUBERNETES_VERSION=\"${CI_VERSION}\"\n    export CI_VERSION\n    # AzureMachineTemplate spec.template.spec is immutable. To keep the\n    # script idempotent across re-runs we persist a generated SSH key as\n    # a Secret in the mgmt cluster and reuse it. The base manifests\n    # interpolate ${AZURE_SSH_PUBLIC_KEY_B64:=\"\"} into\n    # AzureMachineTemplate.spec.template.spec.sshPublicKey; an empty\n    # value triggers CAPZ's defaulter to generate a random key on first\n    # apply, then later applies (with empty) fail the immutability\n    # webhook.\n    export AZURE_SSH_PUBLIC_KEY_B64=\"$(ensure_ssh_keypair)\"\n    export K8S_FEATURE_GATES=\"${K8S_FEATURE_GATES:-AllAlpha=true,AllBeta=true}\"\n    export K8S_RUNTIME_CONFIG=\"${K8S_RUNTIME_CONFIG:-api/all=true}\"\n    export CONTROL_PLANE_MACHINE_COUNT=\"${CONTROL_PLANE_MACHINE_COUNT:-1}\"\n    export WORKER_MACHINE_COUNT=\"${WORKER_MACHINE_COUNT:-2}\"\n    # Internal-only vars; harmless if unused by the public overlay.\n    export AZURE_VNET_CIDR=\"${AZURE_VNET_CIDR:-}\"\n    export AZURE_CP_SUBNET_CIDR=\"${AZURE_CP_SUBNET_CIDR:-}\"\n    export AZURE_NODE_SUBNET_CIDR=\"${AZURE_NODE_SUBNET_CIDR:-}\"\n    export AZURE_INTERNAL_LB_PRIVATE_IP=\"${AZURE_INTERNAL_LB_PRIVATE_IP:-}\"\n    export APISERVER_LB_DNS_SUFFIX=\"${APISERVER_LB_DNS_SUFFIX:-apiserver}\"\n\n    kustomize build \"${overlay}\" > /tmp/cluster-rendered.yaml\n    # Use clusterctl (not envsubst) for variable substitution: clusterctl\n    # understands ${VAR:=default} and correctly converts $${VAR} (escape)\n    # back to a literal ${VAR} for runtime bash to expand on the nodes.\n    clusterctl generate yaml --from /tmp/cluster-rendered.yaml | kubectl apply -f -\n}\n\napply_feature_recipes() {\n    local recipes=\"${EXTRA_FEATURE_RECIPES:-}\"\n    if [[ -z \"${recipes}\" ]]; then\n        return 0\n    fi\n    echo \"--- applying feature recipes: ${recipes} ---\"\n    for recipe in ${recipes}; do\n        local file=\"features/${recipe}.yaml\"\n        if [[ ! -f \"${file}\" ]]; then\n            echo \"feature recipe '${recipe}' not found at ${file}, skipping\" >&2\n            continue\n        fi\n        # Feature recipes are JSON-patches against the KCP — apply with\n        # kubectl patch, not kubectl apply.\n        kubectl patch kubeadmcontrolplane \"${CLUSTER_NAME}-control-plane\" \\\n            --type json --patch-file \"${file}\"\n    done\n}\n\n# Patches NRMS-Rule-101 (TCP) and NRMS-Rule-103 (UDP) deny rules that\n# MSFT-internal Azure subscriptions auto-apply to every NSG. Delegates to\n# peer-vnets.sh::wait_and_fix_nsg_rules; we just translate our env-var\n# names to that function's contract.\nfix_nrms_nsg_rules() {\n    # peer-vnets.sh iterates over three RGs: (node, mgmt, workload-as-RG).\n    # In tilt+aks-as-mgmt the workload cluster has its own RG named after\n    # CLUSTER_NAME. In our flow workload+mgmt share AKS_RG, so we override\n    # CLUSTER_NAME for the function call so the third iteration becomes an\n    # idempotent duplicate of the second instead of timing out on a\n    # non-existent RG.\n    AKS_NODE_RESOURCE_GROUP=\"${AKS_NODE_RG}\" \\\n    AKS_RESOURCE_GROUP=\"${AKS_RG}\" \\\n    CLUSTER_NAME=\"${AKS_RG}\" \\\n    wait_and_fix_nsg_rules\n}\n\nwait_for_workload_cluster_ready() {\n    echo \"--- waiting for workload cluster to be Available (up to 30m) ---\"\n    # CAPI v1beta2 replaced the legacy `Ready` cluster condition with\n    # `Available`. Older code used --for=condition=Ready which never\n    # resolves on v1.13.x+.\n    kubectl wait cluster \"${CLUSTER_NAME}\" \\\n        -n \"${CLUSTER_NAMESPACE:-default}\" \\\n        --for=condition=Available --timeout=30m\n}\n\nmain \"$@\"\n",
    "nameSuffix": "[uniqueString(resourceGroup().id, deployment().name)]",
    "location": "[resourceGroup().location]",
    "bootstrapForceUpdateTag": "[deployment().name]",
    "effectiveControlPlaneMachineCount": "[if(and(equals(parameters('networkingMode'), 'internal'), less(parameters('controlPlaneMachineCount'), 3)), 3, parameters('controlPlaneMachineCount'))]",
    "aksName": "[format('capz-preview-aks-{0}', variables('nameSuffix'))]",
    "aksNodeRg": "[format('{0}-aks-nodes', resourceGroup().name)]",
    "uamiName": "[format('capz-preview-mi-{0}', variables('nameSuffix'))]",
    "mgmtVnetName": "mgmt-vnet",
    "aksSubnetName": "aks-subnet",
    "workloadVnetName": "workload-vnet",
    "cpSubnetName": "control-plane-subnet",
    "nodeSubnetName": "node-subnet",
    "privateDnsZoneName": "[format('{0}.cloudapp.azure.com', variables('location'))]",
    "apiserverDnsLabel": "apiserver",
    "subscriptionId": "[subscription().subscriptionId]",
    "tenantId": "[subscription().tenantId]",
    "roleContributor": "[subscriptionResourceId('Microsoft.Authorization/roleDefinitions', 'b24988ac-6180-42a0-ab88-20f7382dd24c')]"
  },
  "resources": [
    {
      "condition": "[equals(parameters('networkingMode'), 'internal')]",
      "type": "Microsoft.Network/virtualNetworks",
      "apiVersion": "2023-11-01",
      "name": "[variables('mgmtVnetName')]",
      "location": "[variables('location')]",
      "properties": {
        "addressSpace": {
          "addressPrefixes": [
            "[parameters('mgmtVnetCidr')]"
          ]
        },
        "subnets": [
          {
            "name": "[variables('aksSubnetName')]",
            "properties": {
              "addressPrefix": "[parameters('aksSubnetCidr')]"
            }
          }
        ]
      }
    },
    {
      "condition": "[equals(parameters('networkingMode'), 'internal')]",
      "type": "Microsoft.Network/virtualNetworks",
      "apiVersion": "2023-11-01",
      "name": "[variables('workloadVnetName')]",
      "location": "[variables('location')]",
      "properties": {
        "addressSpace": {
          "addressPrefixes": [
            "[parameters('workloadVnetCidr')]"
          ]
        },
        "subnets": [
          {
            "name": "[variables('cpSubnetName')]",
            "properties": {
              "addressPrefix": "[parameters('workloadCpSubnetCidr')]"
            }
          },
          {
            "name": "[variables('nodeSubnetName')]",
            "properties": {
              "addressPrefix": "[parameters('workloadNodeSubnetCidr')]"
            }
          }
        ]
      }
    },
    {
      "condition": "[equals(parameters('networkingMode'), 'internal')]",
      "type": "Microsoft.Network/virtualNetworks/virtualNetworkPeerings",
      "apiVersion": "2023-11-01",
      "name": "[format('{0}/{1}', variables('mgmtVnetName'), 'mgmt-to-workload')]",
      "properties": {
        "remoteVirtualNetwork": {
          "id": "[resourceId('Microsoft.Network/virtualNetworks', variables('workloadVnetName'))]"
        },
        "allowVirtualNetworkAccess": true,
        "allowForwardedTraffic": true,
        "allowGatewayTransit": false,
        "useRemoteGateways": false
      },
      "dependsOn": [
        "[resourceId('Microsoft.Network/virtualNetworks', variables('mgmtVnetName'))]",
        "[resourceId('Microsoft.Network/virtualNetworks', variables('workloadVnetName'))]"
      ]
    },
    {
      "condition": "[equals(parameters('networkingMode'), 'internal')]",
      "type": "Microsoft.Network/virtualNetworks/virtualNetworkPeerings",
      "apiVersion": "2023-11-01",
      "name": "[format('{0}/{1}', variables('workloadVnetName'), 'workload-to-mgmt')]",
      "properties": {
        "remoteVirtualNetwork": {
          "id": "[resourceId('Microsoft.Network/virtualNetworks', variables('mgmtVnetName'))]"
        },
        "allowVirtualNetworkAccess": true,
        "allowForwardedTraffic": true,
        "allowGatewayTransit": false,
        "useRemoteGateways": false
      },
      "dependsOn": [
        "[resourceId('Microsoft.Network/virtualNetworks', variables('mgmtVnetName'))]",
        "[resourceId('Microsoft.Network/virtualNetworks', variables('workloadVnetName'))]"
      ]
    },
    {
      "condition": "[equals(parameters('networkingMode'), 'internal')]",
      "type": "Microsoft.Network/privateDnsZones",
      "apiVersion": "2024-06-01",
      "name": "[variables('privateDnsZoneName')]",
      "location": "global"
    },
    {
      "condition": "[equals(parameters('networkingMode'), 'internal')]",
      "type": "Microsoft.Network/privateDnsZones/A",
      "apiVersion": "2024-06-01",
      "name": "[format('{0}/{1}', variables('privateDnsZoneName'), format('{0}-{1}', parameters('workloadClusterName'), variables('apiserverDnsLabel')))]",
      "properties": {
        "ttl": 300,
        "aRecords": [
          {
            "ipv4Address": "[parameters('workloadApiserverIlbIp')]"
          }
        ]
      },
      "dependsOn": [
        "[resourceId('Microsoft.Network/privateDnsZones', variables('privateDnsZoneName'))]"
      ]
    },
    {
      "condition": "[equals(parameters('networkingMode'), 'internal')]",
      "type": "Microsoft.Network/privateDnsZones/virtualNetworkLinks",
      "apiVersion": "2024-06-01",
      "name": "[format('{0}/{1}', variables('privateDnsZoneName'), 'link-mgmt')]",
      "location": "global",
      "properties": {
        "virtualNetwork": {
          "id": "[resourceId('Microsoft.Network/virtualNetworks', variables('mgmtVnetName'))]"
        },
        "registrationEnabled": false
      },
      "dependsOn": [
        "[resourceId('Microsoft.Network/virtualNetworks', variables('mgmtVnetName'))]",
        "[resourceId('Microsoft.Network/privateDnsZones', variables('privateDnsZoneName'))]"
      ]
    },
    {
      "condition": "[equals(parameters('networkingMode'), 'internal')]",
      "type": "Microsoft.Network/privateDnsZones/virtualNetworkLinks",
      "apiVersion": "2024-06-01",
      "name": "[format('{0}/{1}', variables('privateDnsZoneName'), 'link-workload')]",
      "location": "global",
      "properties": {
        "virtualNetwork": {
          "id": "[resourceId('Microsoft.Network/virtualNetworks', variables('workloadVnetName'))]"
        },
        "registrationEnabled": false
      },
      "dependsOn": [
        "[resourceId('Microsoft.Network/privateDnsZones', variables('privateDnsZoneName'))]",
        "[resourceId('Microsoft.Network/virtualNetworks', variables('workloadVnetName'))]"
      ]
    },
    {
      "type": "Microsoft.ManagedIdentity/userAssignedIdentities",
      "apiVersion": "2023-01-31",
      "name": "[variables('uamiName')]",
      "location": "[variables('location')]"
    },
    {
      "type": "Microsoft.Authorization/roleAssignments",
      "apiVersion": "2022-04-01",
      "name": "[guid(resourceGroup().id, resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', variables('uamiName')), 'Contributor')]",
      "properties": {
        "principalId": "[reference(resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', variables('uamiName')), '2023-01-31').principalId]",
        "principalType": "ServicePrincipal",
        "roleDefinitionId": "[variables('roleContributor')]"
      },
      "dependsOn": [
        "[resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', variables('uamiName'))]"
      ]
    },
    {
      "type": "Microsoft.ContainerService/managedClusters",
      "apiVersion": "2024-05-01",
      "name": "[variables('aksName')]",
      "location": "[variables('location')]",
      "identity": {
        "type": "SystemAssigned"
      },
      "properties": {
        "kubernetesVersion": "[parameters('aksKubernetesVersion')]",
        "dnsPrefix": "[variables('aksName')]",
        "nodeResourceGroup": "[variables('aksNodeRg')]",
        "enableRBAC": true,
        "oidcIssuerProfile": {
          "enabled": true
        },
        "securityProfile": {
          "workloadIdentity": {
            "enabled": true
          }
        },
        "agentPoolProfiles": [
          {
            "name": "sys",
            "count": "[parameters('aksNodeCount')]",
            "vmSize": "[parameters('aksNodeVmSize')]",
            "mode": "System",
            "osType": "Linux",
            "type": "VirtualMachineScaleSets",
            "vnetSubnetID": "[if(equals(parameters('networkingMode'), 'internal'), format('{0}/subnets/{1}', resourceId('Microsoft.Network/virtualNetworks', variables('mgmtVnetName')), variables('aksSubnetName')), null())]"
          }
        ],
        "networkProfile": {
          "networkPlugin": "azure",
          "serviceCidr": "10.0.0.0/16",
          "dnsServiceIP": "10.0.0.10"
        }
      },
      "dependsOn": [
        "[resourceId('Microsoft.Network/virtualNetworks', variables('mgmtVnetName'))]"
      ]
    },
    {
      "type": "Microsoft.ManagedIdentity/userAssignedIdentities/federatedIdentityCredentials",
      "apiVersion": "2023-01-31",
      "name": "[format('{0}/{1}', variables('uamiName'), 'capz-federated-identity')]",
      "properties": {
        "issuer": "[reference(resourceId('Microsoft.ContainerService/managedClusters', variables('aksName')), '2024-05-01').oidcIssuerProfile.issuerURL]",
        "subject": "system:serviceaccount:capz-system:capz-manager",
        "audiences": [
          "api://AzureADTokenExchange"
        ]
      },
      "dependsOn": [
        "[resourceId('Microsoft.ContainerService/managedClusters', variables('aksName'))]",
        "[resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', variables('uamiName'))]"
      ]
    },
    {
      "type": "Microsoft.ManagedIdentity/userAssignedIdentities/federatedIdentityCredentials",
      "apiVersion": "2023-01-31",
      "name": "[format('{0}/{1}', variables('uamiName'), 'aso-federated-identity')]",
      "properties": {
        "issuer": "[reference(resourceId('Microsoft.ContainerService/managedClusters', variables('aksName')), '2024-05-01').oidcIssuerProfile.issuerURL]",
        "subject": "system:serviceaccount:capz-system:azureserviceoperator-default",
        "audiences": [
          "api://AzureADTokenExchange"
        ]
      },
      "dependsOn": [
        "[resourceId('Microsoft.ContainerService/managedClusters', variables('aksName'))]",
        "[resourceId('Microsoft.ManagedIdentity/userAssignedIdentities/federatedIdentityCredentials', variables('uamiName'), 'capz-federated-identity')]",
        "[resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', variables('uamiName'))]"
      ]
    },
    {
      "type": "Microsoft.Resources/deploymentScripts",
      "apiVersion": "2023-08-01",
      "name": "[format('capz-preview-bootstrap-{0}', variables('nameSuffix'))]",
      "location": "[variables('location')]",
      "kind": "AzureCLI",
      "identity": {
        "type": "UserAssigned",
        "userAssignedIdentities": {
          "[format('{0}', resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', variables('uamiName')))]": {}
        }
      },
      "properties": {
        "azCliVersion": "2.61.0",
        "timeout": "PT2H",
        "retentionInterval": "PT1H",
        "cleanupPreference": "OnSuccess",
        "forceUpdateTag": "[variables('bootstrapForceUpdateTag')]",
        "environmentVariables": [
          {
            "name": "AKS_NAME",
            "value": "[variables('aksName')]"
          },
          {
            "name": "AKS_RG",
            "value": "[resourceGroup().name]"
          },
          {
            "name": "AKS_NODE_RG",
            "value": "[variables('aksNodeRg')]"
          },
          {
            "name": "UAMI_CLIENT_ID",
            "value": "[reference(resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', variables('uamiName')), '2023-01-31').clientId]"
          },
          {
            "name": "AZURE_LOCATION",
            "value": "[variables('location')]"
          },
          {
            "name": "AZURE_SUBSCRIPTION_ID",
            "value": "[variables('subscriptionId')]"
          },
          {
            "name": "AZURE_TENANT_ID",
            "value": "[variables('tenantId')]"
          },
          {
            "name": "CLUSTER_NAME",
            "value": "[parameters('workloadClusterName')]"
          },
          {
            "name": "CLUSTER_NAMESPACE",
            "value": "default"
          },
          {
            "name": "CI_VERSION",
            "value": "[parameters('ciVersion')]"
          },
          {
            "name": "K8S_FEATURE_GATES",
            "value": "[if(empty(parameters('featureGatesOverride')), 'AllAlpha=true,AllBeta=true', parameters('featureGatesOverride'))]"
          },
          {
            "name": "K8S_RUNTIME_CONFIG",
            "value": "api/all=true"
          },
          {
            "name": "CONTROL_PLANE_MACHINE_COUNT",
            "value": "[string(variables('effectiveControlPlaneMachineCount'))]"
          },
          {
            "name": "WORKER_MACHINE_COUNT",
            "value": "[string(parameters('workerMachineCount'))]"
          },
          {
            "name": "AZURE_CONTROL_PLANE_MACHINE_TYPE",
            "value": "[parameters('workloadControlPlaneVmSize')]"
          },
          {
            "name": "AZURE_NODE_MACHINE_TYPE",
            "value": "[parameters('workloadNodeVmSize')]"
          },
          {
            "name": "NETWORKING_MODE",
            "value": "[parameters('networkingMode')]"
          },
          {
            "name": "AZURE_VNET_CIDR",
            "value": "[parameters('workloadVnetCidr')]"
          },
          {
            "name": "AZURE_CP_SUBNET_CIDR",
            "value": "[parameters('workloadCpSubnetCidr')]"
          },
          {
            "name": "AZURE_NODE_SUBNET_CIDR",
            "value": "[parameters('workloadNodeSubnetCidr')]"
          },
          {
            "name": "AZURE_INTERNAL_LB_PRIVATE_IP",
            "value": "[parameters('workloadApiserverIlbIp')]"
          },
          {
            "name": "APISERVER_LB_DNS_SUFFIX",
            "value": "[variables('apiserverDnsLabel')]"
          },
          {
            "name": "PREVIEW_REPO_URL",
            "value": "[parameters('previewRepoUrl')]"
          },
          {
            "name": "PREVIEW_REPO_REF",
            "value": "[parameters('previewRepoRef')]"
          },
          {
            "name": "EXTRA_FEATURE_RECIPES",
            "value": "[parameters('extraFeatureRecipes')]"
          }
        ],
        "scriptContent": "[variables('$fxv#0')]"
      },
      "dependsOn": [
        "[resourceId('Microsoft.ManagedIdentity/userAssignedIdentities/federatedIdentityCredentials', variables('uamiName'), 'aso-federated-identity')]",
        "[resourceId('Microsoft.ManagedIdentity/userAssignedIdentities/federatedIdentityCredentials', variables('uamiName'), 'capz-federated-identity')]",
        "[resourceId('Microsoft.Network/virtualNetworks/virtualNetworkPeerings', variables('mgmtVnetName'), 'mgmt-to-workload')]",
        "[resourceId('Microsoft.Network/virtualNetworks/virtualNetworkPeerings', variables('workloadVnetName'), 'workload-to-mgmt')]",
        "[resourceId('Microsoft.Network/privateDnsZones/A', variables('privateDnsZoneName'), format('{0}-{1}', parameters('workloadClusterName'), variables('apiserverDnsLabel')))]",
        "[resourceId('Microsoft.Network/privateDnsZones/virtualNetworkLinks', variables('privateDnsZoneName'), 'link-mgmt')]",
        "[resourceId('Microsoft.Network/privateDnsZones/virtualNetworkLinks', variables('privateDnsZoneName'), 'link-workload')]",
        "[resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', variables('uamiName'))]",
        "[resourceId('Microsoft.Authorization/roleAssignments', guid(resourceGroup().id, resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', variables('uamiName')), 'Contributor'))]"
      ]
    }
  ],
  "outputs": {
    "resourceGroupName": {
      "type": "string",
      "metadata": {
        "description": "Resource group the deployment landed in. Use with az aks get-credentials."
      },
      "value": "[resourceGroup().name]"
    },
    "aksClusterName": {
      "type": "string",
      "metadata": {
        "description": "AKS management cluster name. Pull its kubeconfig with `az aks get-credentials -g <rg> -n <aksName> --admin --overwrite-existing`."
      },
      "value": "[variables('aksName')]"
    },
    "workloadClusterName": {
      "type": "string",
      "metadata": {
        "description": "Workload cluster name (CAPI Cluster CR name in the mgmt cluster). After getting AKS credentials, the workload kubeconfig is in the `default` namespace as the Secret `<workloadClusterName>-kubeconfig`."
      },
      "value": "[parameters('workloadClusterName')]"
    },
    "cleanupCommand": {
      "type": "string",
      "metadata": {
        "description": "Command to delete everything created by this template."
      },
      "value": "[format('az group delete --name {0} --yes --no-wait', resourceGroup().name)]"
    }
  }
}
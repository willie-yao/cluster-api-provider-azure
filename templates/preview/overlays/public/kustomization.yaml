apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
# Default networking: CAPZ creates the workload VNet and a public LB
# frontend on the apiserver. No additional patches needed.
resources:
- ../../base

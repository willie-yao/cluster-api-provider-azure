apiVersion: addons.cluster.x-k8s.io/v1alpha1
kind: HelmChartProxy
metadata:
  name: cloud-provider-azure-chart
spec:
  valuesTemplate: |
    infra:
      clusterName: {{ .Cluster.metadata.name }}
    cloudControllerManager:
      clusterCIDR: {{ .Cluster.spec.clusterNetwork.pods.cidrBlocks | join "," }}
      logVerbosity: 4
      imageName: "${CCM_IMAGE_NAME:-azure-cloud-controller-manager}"
      imageRepository: "${IMAGE_REGISTRY:-mcr.microsoft.com/oss/kubernetes}"
      imageTag: "${CCM_IMAGE_TAG:-v1.34.0}"
    cloudNodeManager:
      imageName: "${CNM_IMAGE_NAME:-azure-cloud-node-manager}"
      imageRepository: "${IMAGE_REGISTRY:-mcr.microsoft.com/oss/kubernetes}"
      imageTag: "${CNM_IMAGE_TAG:-v1.34.0}"